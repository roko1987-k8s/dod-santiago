apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: developer-pod-operator
  namespace: demo
rules:
  - apiGroups: [""]
    resources:
      - pods
    verbs:
      - get
      - list
      - watch
